- 描述:右边点击,弹框是否删除当前行,确定?取消?
- a.事件
```
row-contextmenu 返回:row, event

event.preventDefault() //阻止默认事件
```
- b.弹窗
```			
<!-- 删除确认-->
<el-dialog title="提示" class="sfsc" :visible.sync="sfsc_Click" size="tiny" :before-close="handleClose" >
<span slot="footer" class="dialog-footer">
  <el-button @click="sfsc_Click = false">取 消</el-button>
  <el-button type="primary"  @click="isDelect" >确 定</el-button>
</span>
</el-dialog>
<!--删除确认 end -->
```
- c.isDelect 函数
```

// 行删除
rowDelect (row, event) {
  // console.log(row,event);
  console.log( $(event.target.parentNode.parentNode)); //获取等坐标
  event.preventDefault() //阻止默认事件
  this.sfsc_Click = true;
  this.delIndex = $(event.target).parent().parent().parent().index(); //获取等坐标
  // console.log('this.delIndex:');
  // console.log(this.delIndex);
},
// 是否删除
isDelect () {
  // console.log(this.splbs);
  this.sfsc_Click = false; // 关闭弹窗
  this.splbs.splice(this.delIndex,1); // 删除数据
  if(this.splbs < 1) { // 如果全部删除
    this.splbs.push({Code:''});
  }
},
```
- d.css样式
```
/* 是否删除弹窗 */
.sfsc .el-dialog--tiny {
	width: 15%;
	text-align: center;
	padding-bottom: 1rem;
}
.sfsc .el-dialog--tiny .el-button--primary{
	margin-right: 2rem;
}
.sfsc .el-dialog--tiny p {
	font-size: 1.2rem;
	color: #000;
	line-height: 58px;
}
```